const assert = require('assert');

describe('Sample API suit', () => {
    it('should be ok', () => {
        const expect = {
            id: 1,
            value: 'tempValue'
        };
        assert.deepEqual(expect, false);
    })
})